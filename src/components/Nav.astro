---
const links = [{ label: "Wykłady", link: "/wyklady" }];
---

<>
  <nav class="w-full py-8 px-6 text-lg font-bold border-b-2 border-b-orange-400 mb-12">
    <div class="flex items-center justify-between">
      <h1>UniBase</h1>
      <div>
        {
          links.map((link) => (
            <div>
              <a href={link.link}>{link.label}</a>
            </div>
          ))
        }
      </div>
    </div>
  </nav>
</>
