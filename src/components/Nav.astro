---
const links = [
  { label: "Wykłady", link: "#wyklady" },
  { label: "Egzaminy", link: "#zaliczenia" },
];
---

<nav class="w-full py-8 px-6 md:text-lg font-bold border-b-2 border-b-orange-400 mb-12 uppercase">
  <div class="flex items-center justify-between">
    <h1 class="hidden sm:inline"><a href="/">UniBase</a></h1>
    <div class="flex flex-wrap gap-y-2 gap-x-4 lg:gap-x-8">
      {
        links.map((link) => (
          <div>
            <a href={link.link}>{link.label}</a>
          </div>
        ))
      }
    </div>
  </div>
</nav>
